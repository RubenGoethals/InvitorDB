@model IEnumerable<Event>

@{
    ViewData["Title"] = "Home Page";
}

<h3>Events</h3>

<p>
    <partial name="Form_EventsForEvent.cshtml" />
</p>

<section class="card-deck">
    @foreach (var item in Model)
    {
        <partial name="_DetailsEventPartial" model="item" />
    }
</section>


